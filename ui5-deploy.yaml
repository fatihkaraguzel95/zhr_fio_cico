# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: ZHR_FIO_CICO
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: https://SAPDEV1.PORTA.DE:8201/sap/opu/odata/sap/ZHR_XSS_CICO_SRV
          client: "100"
        app:
          name: ZHR_FIO_CICO
          description: Schnellbuchung Kommen/Gehen
          package: ZHR_XSS_FIORI
          transport: DHRK914851
        exclude:
          - /test/
